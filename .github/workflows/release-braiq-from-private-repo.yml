# ========================================
# Workflow GitHub Actions pour BRAIQ
# À placer dans : oktra/apps/.github/workflows/release-braiq.yml
# ========================================
#
# Ce workflow :
# 1. ✅ Checkout le repo PRIVÉ BRAIQ (via token)
# 2. ✅ Build depuis le repo privé
# 3. ✅ Signe et notarise (si configuré)
# 4. ✅ Crée la release dans oktra/apps (PUBLIC)
#
# Architecture :
# - Repo PRIVÉ : Votre repo BRAIQ (code source)
# - Repo PUBLIC : oktra/apps (releases uniquement)
#
# ========================================

name: Release BRAIQ macOS

on:
  push:
    tags:
      - 'braiq-v*'  # Tags spécifiques à BRAIQ (ex: braiq-v1.0.0)
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., v1.0.0)'
        required: true
      braiq_ref:
        description: 'Branch ou tag du repo BRAIQ privé (ex: main, v1.0.0)'
        required: false
        default: 'main'

jobs:
  build-macos:
    runs-on: macos-latest
    
    strategy:
      matrix:
        include:
          - target: aarch64-apple-darwin
            arch: arm64
          - target: x86_64-apple-darwin
            arch: x64

    steps:
      # ⚠️ IMPORTANT : Checkout le repo PRIVÉ BRAIQ
      - name: Checkout BRAIQ private repository
        uses: actions/checkout@v4
        with:
          repository: ${{ secrets.BRAIQ_REPO_OWNER }}/${{ secrets.BRAIQ_REPO_NAME }}
          ref: ${{ github.event.inputs.braiq_ref || 'main' }}
          token: ${{ secrets.BRAIQ_REPO_TOKEN }}
          path: BRAIQ  # Checkout dans un sous-dossier

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: BRAIQ/package-lock.json

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install dependencies
        working-directory: ./BRAIQ
        run: npm ci

      - name: Build frontend
        working-directory: ./BRAIQ
        run: npm run build

      # Option 1: Build sans signature
      - name: Build Tauri app (unsigned)
        working-directory: ./BRAIQ
        if: ${{ !secrets.APPLE_SIGNING_IDENTITY }}
        run: |
          # Build seulement l'app (pas le DMG pour éviter les erreurs)
          jq '.tauri.bundle.targets = ["app"]' \
            src-tauri/tauri.conf.json > src-tauri/tauri.conf.json.tmp && mv src-tauri/tauri.conf.json.tmp src-tauri/tauri.conf.json
          
          npx tauri build --target ${{ matrix.target }}

      # Option 2: Build avec signature
      - name: Import Code Signing Certificate
        if: ${{ secrets.APPLE_CERTIFICATE_BASE64 && secrets.APPLE_CERTIFICATE_PASSWORD }}
        uses: apple-actions/import-codesign-certs@v2
        with:
          p12-file-base64: ${{ secrets.APPLE_CERTIFICATE_BASE64 }}
          p12-password: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}

      - name: Build Tauri app (signed)
        working-directory: ./BRAIQ
        if: ${{ secrets.APPLE_SIGNING_IDENTITY }}
        run: |
          # Modifier temporairement tauri.conf.json pour ajouter signingIdentity
          jq '.tauri.bundle.macOS.signingIdentity = "${{ secrets.APPLE_SIGNING_IDENTITY }}"' \
            src-tauri/tauri.conf.json > src-tauri/tauri.conf.json.tmp && mv src-tauri/tauri.conf.json.tmp src-tauri/tauri.conf.json
          
          # Build seulement l'app (pas le DMG pour éviter les erreurs)
          jq '.tauri.bundle.targets = ["app"]' \
            src-tauri/tauri.conf.json > src-tauri/tauri.conf.json.tmp && mv src-tauri/tauri.conf.json.tmp src-tauri/tauri.conf.json
          
          npx tauri build --target ${{ matrix.target }}

      # Créer le DMG automatiquement après le build
      - name: Create DMG automatically
        working-directory: ./BRAIQ
        run: |
          APP_PATH="src-tauri/target/${{ matrix.target }}/release/bundle/macos/BRAIQ.app"
          DMG_DIR="src-tauri/target/${{ matrix.target }}/release/bundle/dmg"
          DMG_PATH="$DMG_DIR/BRAIQ_1.0.0_${{ matrix.arch }}.dmg"

          if [ -d "$APP_PATH" ]; then
            echo "Creating DMG automatically from $APP_PATH..."
            mkdir -p "$DMG_DIR"

            # Méthode 1: Essayer avec hdiutil directement
            echo "Trying hdiutil method..."
            if hdiutil create -fs HFS+ -srcfolder "$APP_PATH" -volname "BRAIQ" "$DMG_PATH" 2>/dev/null; then
              echo "✅ DMG created successfully with hdiutil"
              ls -lh "$DMG_PATH"
            else
              # Méthode 2: Script personnalisé en mode sandbox-safe
              echo "Falling back to sandbox-safe method..."
              chmod +x scripts/create-dmg.sh
              # Utiliser le script en mode sandbox-safe
              sed -i '' 's/hdiutil create.*$/hdiutil makehybrid -default-volume-name "$VOLUME_NAME" -hfs -o "${DMG_TEMP_NAME}" "$SRC_FOLDER" 2>\/dev\/null || { rm -f "${DMG_TEMP_NAME}"; echo "DMG creation failed in sandbox"; exit 1; }/' scripts/create-dmg.sh
              if ./scripts/create-dmg.sh "$DMG_PATH" "$APP_PATH" "BRAIQ" 2>/dev/null; then
                echo "✅ DMG created successfully with fallback method"
                ls -lh "$DMG_PATH"
              else
                echo "⚠️ DMG creation failed, but app is available for manual packaging"
                # Créer au moins un fichier placeholder pour éviter les erreurs downstream
                touch "$DMG_PATH.placeholder"
              fi
            fi
          else
            echo "❌ Error: App not found at $APP_PATH"
            exit 1
          fi

      # Option 3: Notarisation
      - name: Notarize app
        working-directory: ./BRAIQ
        if: ${{ secrets.APPLE_ID && secrets.APPLE_ID_PASSWORD && secrets.APPLE_TEAM_ID }}
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_ID_PASSWORD: ${{ secrets.APPLE_ID_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          chmod +x scripts/notarize-mac-app.sh
          ./scripts/notarize-mac-app.sh src-tauri/target/${{ matrix.target }}/release/bundle/macos/BRAIQ.app

      - name: Prepare artifacts
        working-directory: ./BRAIQ
        run: |
          mkdir -p artifacts
          
          # Copier l'app
          if [ -d "src-tauri/target/${{ matrix.target }}/release/bundle/macos/BRAIQ.app" ]; then
            ditto -c -k --keepParent \
              "src-tauri/target/${{ matrix.target }}/release/bundle/macos/BRAIQ.app" \
              "artifacts/BRAIQ-macos-${{ matrix.arch }}.app.zip"
          fi
          
          # Copier le DMG créé automatiquement (ou gérer l'absence)
          DMG_PATH="src-tauri/target/${{ matrix.target }}/release/bundle/dmg/BRAIQ_1.0.0_${{ matrix.arch }}.dmg"
          if [ -f "$DMG_PATH" ]; then
            cp "$DMG_PATH" "artifacts/BRAIQ-macos-${{ matrix.arch }}.dmg"
            echo "✅ DMG copied to artifacts"
          elif [ -f "${DMG_PATH}.placeholder" ]; then
            echo "⚠️ DMG creation failed, but app is available for manual packaging"
            # Copier l'app avec un nom spécial pour indiquer qu'il faut créer le DMG manuellement
            cp "artifacts/BRAIQ-macos-${{ matrix.arch }}.app.zip" "artifacts/BRAIQ-macos-${{ matrix.arch }}.app.NEEDS_DMG.zip"
          else
            echo "⚠️ Warning: DMG not found at $DMG_PATH - will use app only"
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: BRAIQ-macos-${{ matrix.arch }}
          path: BRAIQ/artifacts/*
          if-no-files-found: error

  create-release:
    needs: build-macos
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    
    steps:
      # Checkout oktra/apps pour créer la release ici
      - name: Checkout oktra/apps repository
        uses: actions/checkout@v4
        with:
          path: oktra-apps

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure of downloaded files
        run: ls -R artifacts

      - name: Create Release in oktra/apps
        uses: softprops/action-gh-release@v1
        with:
          repository: oktra/apps  # ⚠️ Créer la release dans oktra/apps
          draft: true
          files: |
            artifacts/**/*.dmg
            artifacts/**/*.zip
          body: |
            ## 📦 BRAIQ Release
            
            Téléprompteur intelligent pour macOS
            
            ### Installation
            
            #### macOS
            
            **⚠️ Important** : Si vous voyez le message "BRAIQ est endommagé", c'est normal !
            
            Exécutez simplement cette commande dans le Terminal :
            ```bash
            xattr -d com.apple.quarantine /Applications/BRAIQ.app
            ```
            
            ### Téléchargements

            - **macOS Apple Silicon (M1/M2/M3/M4)** :
              - `BRAIQ-macos-arm64.dmg` (DMG recommandé)
              - `BRAIQ-macos-arm64.app.zip` (si DMG non disponible)
            - **macOS Intel** :
              - `BRAIQ-macos-x64.dmg` (DMG recommandé)
              - `BRAIQ-macos-x64.app.zip` (si DMG non disponible)
            
            ### Notes de version
            
            _Ajoutez vos notes de version ici_
            
            ---
            
            🔗 Release créée automatiquement depuis le repo privé BRAIQ
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

# ========================================
# Configuration des Secrets dans oktra/apps
# ========================================
#
# Allez sur : https://github.com/oktra/apps/settings/secrets/actions
#
# Secrets REQUIS pour accéder au repo privé BRAIQ :
# - BRAIQ_REPO_TOKEN        : Token GitHub avec accès au repo privé BRAIQ
# - BRAIQ_REPO_OWNER         : Owner du repo (ex: "votre-username")
# - BRAIQ_REPO_NAME          : Nom du repo privé (ex: "screen-whisper-presenter")
#
# Secrets pour la signature (Option 2) :
# - APPLE_CERTIFICATE_BASE64
# - APPLE_CERTIFICATE_PASSWORD
# - APPLE_SIGNING_IDENTITY
#
# Secrets pour la notarisation (Option 3) :
# - APPLE_ID
# - APPLE_ID_PASSWORD
# - APPLE_TEAM_ID
#
# ========================================
# Comment créer BRAIQ_REPO_TOKEN
# ========================================
#
# 1. Allez sur https://github.com/settings/tokens
# 2. Cliquez sur "Generate new token" → "Generate new token (classic)"
# 3. Donnez un nom : "BRAIQ GitHub Actions"
# 4. Sélectionnez les scopes :
#    - ✅ repo (Full control of private repositories)
# 5. Cliquez sur "Generate token"
# 6. Copiez le token (vous ne le reverrez plus !)
# 7. Ajoutez-le dans oktra/apps comme secret BRAIQ_REPO_TOKEN
#
# ========================================
# Utilisation
# ========================================
#
# Pour créer une release depuis oktra/apps :
#
# cd oktra/apps
# git tag braiq-v1.0.0
# git push origin braiq-v1.0.0
#
# Le workflow va automatiquement :
# 1. Checkout le repo privé BRAIQ (branche main par défaut)
# 2. Builder pour arm64 et x64
# 3. Signer (si secrets configurés)
# 4. Notariser (si tous les secrets configurés)
# 5. Créer une release draft dans oktra/apps
#
# Pour builder depuis une branche/tag spécifique :
# - Allez dans Actions → Release BRAIQ macOS → Run workflow
# - Entrez la version (ex: v1.0.0)
# - Entrez la ref (ex: main, develop, v1.0.0)
#
# ========================================

